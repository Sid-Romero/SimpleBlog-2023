<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.SimpleBlog" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Couleurs générales de l'application -->
        <item name="colorPrimary">@color/white</item> <!-- Couleur de fond des boutons -->
        <item name="colorOnPrimary">@color/black</item> <!-- Couleur du texte des boutons -->
    </style>

    <style name="Theme.SimpleBlog" parent="Base.Theme.SimpleBlog" />

    <!--
    <style name="ListView.SimpleBlog" parent="Widget.AppCompat.ListView">
        <item name="android:background">@color/black</item>
        <item name="android:divider">@color/black</item>
        <item name="android:dividerHeight">2dp</item>
        <item name="android:textColor">@color/black</item>

    </style> -->

    <!-- Style personnalisé pour les boutons réguliers -->
    <style name="Button.SimpleBlog" parent="Widget.Material3.Button">
        <item name="backgroundTint">@color/black
        </item> <!-- Couleur de fond des boutons réguliers -->
        <item name="android:textColor">@color/white
        </item> <!-- Couleur du texte des boutons réguliers -->
    </style>

    <!-- Style personnalisé pour les boutons flottants -->
    <style name="FloatingActionButton.SimpleBlog" parent="Widget.Design.FloatingActionButton">
        <item name="backgroundTint">@color/black</item> <!-- Couleur de fond du bouton flottant -->
        <item name="tint">@color/white</item> <!-- Couleur de l'icône du bouton flottant -->
    </style>


</resources>
